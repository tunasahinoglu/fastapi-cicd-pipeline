{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "fastapi-app.fullname" .) }}
  labels:
    {{- include "fastapi-app.labels" . | nindent 4 }}
{{- end }}